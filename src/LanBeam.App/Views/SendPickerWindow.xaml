<ui:FluentWindow
    x:Class="LanBeam.App.Views.SendPickerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{DynamicResource Str_SendToWho}"
    Width="460" Height="520"
    ResizeMode="CanResize" Topmost="True"
    ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen"
    KeyDown="Window_KeyDown">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="LanBeam" ShowMinimize="False" ShowMaximize="False"
                     Icon="pack://application:,,,/Assets/lanbeam.ico" />

        <Grid Grid.Row="1" Margin="20,8,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{DynamicResource Str_SendToWho}" FontSize="20" FontWeight="SemiBold" />
            <TextBlock Grid.Row="1" x:Name="PathsText" Margin="0,8,0,12" FontSize="13" Opacity="0.7"
                       TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="60" />

            <Grid Grid.Row="2">
                <StackPanel x:Name="EmptyState" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ui:ProgressRing IsIndeterminate="True" Width="40" Height="40" />
                    <TextBlock Margin="0,12,0,0" Opacity="0.7" TextAlignment="Center"
                               Text="{DynamicResource Str_SearchingDevices}" />
                    <TextBlock Margin="0,4,0,0" Opacity="0.5" FontSize="12" TextAlignment="Center"
                               TextWrapping="Wrap" MaxWidth="320"
                               Text="{DynamicResource Str_SendPickerHint}" />
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Devices}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,4" Padding="12" CornerRadius="8"
                                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid Grid.Column="0" Width="36" Height="36">
                                            <Border CornerRadius="18" Background="{Binding PresetBrush}"
                                                    Visibility="{Binding HasImage, Converter={StaticResource InverseBoolToVis}}">
                                                <TextBlock Text="{Binding PresetGlyph}"
                                                           FontFamily="Segoe MDL2 Assets" FontSize="16"
                                                           Foreground="White" HorizontalAlignment="Center"
                                                           VerticalAlignment="Center" />
                                            </Border>
                                            <Ellipse Visibility="{Binding HasImage, Converter={StaticResource BoolToVis}}">
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding AvatarImage}" Stretch="UniformToFill" />
                                                </Ellipse.Fill>
                                            </Ellipse>
                                        </Grid>
                                        <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding Address}" Opacity="0.6" FontSize="11" />
                                        </StackPanel>
                                        <ui:Button Grid.Column="2" Content="{DynamicResource Str_Send}" Appearance="Primary"
                                                   Icon="{ui:SymbolIcon Send24}" VerticalAlignment="Center"
                                                   Click="Send_Click" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <Button Grid.Row="3" Content="{DynamicResource Str_Cancel}" HorizontalAlignment="Right" Margin="0,12,0,0"
                    MinWidth="100" Click="Cancel_Click" />
        </Grid>
    </Grid>
</ui:FluentWindow>
